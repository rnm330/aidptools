# 🐑 小羊 AIDP 做题工具

Chrome 浏览器扩展，让 AIDP 做题轻松一点。

## ✨ 功能一览

**📊 自动计数** — 页面上点「提交」自动 +1，「上一题」自动 -1（禁用/倒计时状态不触发），实时显示进度、完成率和节奏提示

**🏷️ 快捷标签** — 把常用标签保存下来，一键自动填充，告别重复手选

**🐟 摸鱼模式 v2** — 手动/自动摸鱼双模式，60秒刷新 + 静音循环播放视频 + 5秒循环模拟点击选项卡保持活跃，摸鱼自身刷新后自动恢复

**⏱️ 自动摸鱼** — 设置中开启后，30秒无操作或网页进入后台自动进入摸鱼模式；手动停止后不会因操作自动退出（必须手动停止）

**⚙️ 自定义设置** — 可修改每日目标和工作时间段，输入任务 ID 快速跳转

## 🏷️ 快捷标签怎么用

**保存标签：** 在页面上手动选好标签 → 点击「快捷选择」→ 自动保存，浮动窗口随即出现

**新增标签：** 页面上选好新标签 → 点击「快捷选择」→ 选择「新增标签」，自动追加并去重

**替换标签：** 页面上选好标签 → 点击「快捷选择」→ 选择「替换标签」，覆盖之前保存的

**清空标签：** 页面上不要选标签 → 点击「快捷选择」→ 选择「清空标签」

**一键填充：** 浮动窗口中直接点击标签按钮，自动完成级联选择。支持连续点击自动排队、已存在自动跳过、高频标签紫色高亮。窗口可拖拽、缩放、最小化，位置跨天保持稳定。

## 🐟 摸鱼模式怎么用（v0.4 更新）

### 手动摸鱼
点击底部工具栏的「摸鱼」按钮即可开启。开启后会：
- 每 **60 秒**刷新页面（摸鱼自身的刷新会自动恢复摸鱼状态）
- **静音 + 循环播放**视频
- 每 **5 秒**模拟点击下拉框输入框（点第一个 → 点空白关掉 → 点第二个 → 点空白关掉）保持页面活跃
- 停止后恢复视频原始静音和播放状态，并**暂停视频**

### 自动摸鱼
在设置中打开 **「自动摸鱼（空闲30秒后）」** 开关后，满足以下任一条件会**自动进入摸鱼**：

| 触发条件 | 说明 |
|---------|------|
| 空闲 30 秒 | 无鼠标移动、键盘按键、滚轮等任何操作 |
| 网页进入后台 | 切换到其他浏览器标签页 / 最小化窗口 |

### 注意事项 ⚠️
- 摸鱼模式开启后**不会因为用户操作而自动退出**，必须手动点击「停止」按钮
- **不要关闭当前标签页** — 关闭后摸鱼停止
- **不要提交题目** — 提交会计入做题计数
- **不要打开左侧侧边栏** — 可能导致页面行为异常
- **不要手动刷新页面（F5）** — 手动刷新会停止摸鱼（只有摸鱼自己的60s倒计时刷新才会保留状态）

### 多窗口独立
每个浏览器标签页的摸鱼状态独立存储（sessionStorage），互不影响。

## 📦 安装

1. 下载 [Releases](https://github.com/rnm330/aidptools/releases) 中的 zip 并解压
2. 打开 `chrome://extensions/`
3. 开启「开发者模式」
4. 点击「加载已解压的扩展程序」，选择解压后的文件夹
5. 上传到 Chrome 商店时需要填写隐私权声明（见下方）

### Chrome 商店隐私权说明

**host_permissions：** 仅请求访问 `https://aidp.bytedance.com/management/task-v2/*`，用于在 AIDP 平台注入内容脚本、读取 DOM 元素和注入自定义 UI。不会收集用户数据或向外部发送请求。

**storage：** 用于在本地保存配置和状态数据（每日目标、工作时间、已完成计数、快捷标签模板、窗口位置等）。所有数据仅存于本地浏览器，不涉及网络传输。

## 更新日志

### v0.4 — 摸鱼模式大升级 + 上一题修复

**🐟 摸鱼模式调整：**
- 刷新间隔调整为 **60 秒**
- 新增 **自动摸鱼**功能：设置中可开关，满足以下条件自动触发：
  - 空闲 30 秒无任何操作（鼠标/键盘/滚轮）
  - 网页切换到后台（切标签页/最小化窗口）
- 模拟点击升级：每 **5 秒**一轮 4 步循环（点击下拉框 → 点空白 → 点第二个下拉框 → 点空白）
- 视频处理优化：开始时静音+循环播放；停止时**恢复静音状态并暂停视频**
- 摸鱼模式**不会因用户操作自动退出**，必须手动停止
- 摸鱼自身的 60s 刷新会自动恢复摸鱼状态（手动 F5 则停止）
- 新增安全提示文字

**🔢 上一题 -1 修复：**
- 完全重写检测逻辑，精确匹配"上一题"按钮（支持倒计时文本如 `上一题 (109)s`）
- 通过 `btn-hover` class 容器识别真实按钮，排除页面其他文本误匹配
- 禁用状态检测：`prev-disabled` 样式时不触发 -1
- 彻底排除插件自身点击和 AutoFiller 填充触发误计数

**🔧 其他：**
- 插件生效范围收窄至 `/management/task-v2/*` 前缀
- 输入框防抖优化为 150ms
- 快捷选择窗口使用 `getBoundingClientRect()` 保持位置跨天稳定

---

**v0.3.1** — 修复样式覆盖、摸鱼刷新保持、多窗口独立
**v0.3** — 新增摸鱼模式，UI 主题升级
**v0.2** — 新增快捷标签填充
**v0.1** — 进度追踪与智能计算

## 许可证

MIT
